import React from 'react';
import ReactDOM from 'react-dom/client';
import { ContainerPage } from '../src/pages/containerPage.jsx'
const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <ContainerPage />
);